审查 $ARGUMENTS 的代码质量，按以下清单逐项检查：

## 检查清单

1. **Bug 风险**：是否有空指针、越界、类型错误？
2. **安全性**：是否有硬编码密钥、SQL 注入、XSS 风险？
3. **性能**：是否有不必要的循环、重复计算、内存泄漏？
4. **可读性**：命名是否清晰？函数是否过长？
5. **测试**：关键逻辑是否有测试覆盖？

## 输出格式

对每个发现的问题，按这个格式输出：
- **文件:行号** — 问题描述
- **严重程度**：高 / 中 / 低
- **修复建议**：具体的代码修改方案
